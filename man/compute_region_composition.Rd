% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualize.R
\name{compute_region_composition}
\alias{compute_region_composition}
\title{Compute composition for DMCs and/or DMRs from merged bedMethyl counts}
\usage{
compute_region_composition(
  merged,
  pheno_file,
  subject_id_var = "subject_id",
  group_var = "group",
  group_levels = c("control", "case"),
  dmcs = NULL,
  dmrs = NULL,
  strip_sample_prefix_regex = NULL
)
}
\arguments{
\item{merged}{A data.frame from \code{\link{merge_data}} containing \code{Coord}
and per-sample columns \code{Methyl_*}, \code{Unmethyl_*}, \code{Hydroxymethyl_*}.}

\item{pheno_file}{Path to a CSV phenotype file.}

\item{subject_id_var}{Phenotype column giving sample IDs (must match merged suffixes).}

\item{group_var}{Phenotype column defining groups (e.g., Cohort, Group).}

\item{group_levels}{Character vector of length 2 specifying the two groups to include,
in plotting order.}

\item{dmcs}{Optional data.frame with \code{chr} and \code{start} to subset site results.}

\item{dmrs}{Optional data.frame with \code{chr}, \code{start}, \code{end} defining regions.}

\item{strip_sample_prefix_regex}{Optional regex to strip from merged sample suffixes
(e.g., "^SS" to turn "SS10208" into "10208").}
}
\value{
A list with:
\describe{
\item{site_props}{All site-level compositions for both groups (data.frame).}
\item{dmc_data}{Site compositions restricted to dmcs (or NULL).}
\item{dmr_data}{Region compositions restricted to dmrs (or NULL).}
}
}
\description{
Computes group-wise composition (methyl, unmodified, hydroxy) at CpG sites
from a merged bedMethyl table, then optionally aggregates site composition
into DMRs by averaging CpGs that overlap each region.
}
