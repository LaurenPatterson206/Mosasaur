% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate_parameters.R
\name{Estimate_Parameters}
\alias{Estimate_Parameters}
\title{Estimate Dirichlet alpha parameters from raw bedMethyl counts (fast streaming)}
\usage{
Estimate_Parameters(
  path = ".",
  pattern = "\\\\\\\\.bed$",
  keep_mod_code = "m",
  pseudocount = 0,
  subsample = NULL,
  seed = NULL,
  categories = c("methylated", "unmodified", "hydroxymethylated")
)
}
\arguments{
\item{path}{Directory containing bedMethyl files. Defaults to \code{"."}.}

\item{pattern}{Regex to match bedMethyl files. Defaults to \code{"\\\\\\\\.bed$"}.}

\item{keep_mod_code}{Value in column V4 to keep. Defaults to \code{"m"}.}

\item{pseudocount}{Numeric added to each count before forming proportions.
Helps stabilize estimation when many zeros exist. Default \code{0}.}

\item{subsample}{Integer or NULL. If provided, randomly samples up to this
many observations per file (after filtering \code{V4}). Default \code{NULL}.}

\item{seed}{Optional RNG seed used with \code{subsample}.}

\item{categories}{Names for the three returned alpha parameters.}
}
\value{
A list with:
\describe{
\item{alpha}{Named numeric vector of length 3.}
\item{alpha1,alpha2,alpha3}{Convenience scalars.}
\item{p_mean}{Mean proportions for each category.}
\item{p_sd}{SD of proportions for each category.}
\item{n_obs}{Number of observations used.}
\item{alpha0}{Estimated concentration (sum of alphas).}
}
}
\description{
Estimates Dirichlet parameters for three categories (methylated,
unmodified, hydroxymethylated) from raw bedMethyl count data using
a method-of-moments approach based on empirical means and SDs of proportions
}
