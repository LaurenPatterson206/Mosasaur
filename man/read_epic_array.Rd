% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_epic_array.R
\name{read_epic_array}
\alias{read_epic_array}
\title{Read and convert EPIC methylation array beta values to Mosasaur count matrices}
\usage{
read_epic_array(
  beta_file,
  sample_info_file,
  annotation_file,
  perfect_matches_file = NULL,
  beta_probe_id_col = "TargetID",
  anno_probe_id_col = "IlmnID",
  anno_chr_col = "CHR",
  anno_pos_col = "MAPINFO",
  anno_gene_col = "UCSC_RefGene_Name",
  sample_id_col = "Sample_ID",
  liftover_chain = NULL,
  liftover_seqstyle = "UCSC",
  probe_width = 40L,
  coverage_per_probe = 500L,
  write_outputs = TRUE,
  prefix = "EPIC",
  return_annotation = TRUE
)
}
\arguments{
\item{beta_file}{Path to a tab-delimited table of beta values with one row per
probe and one column per sample, plus a probe ID column.}

\item{sample_info_file}{Path to a CSV of sample metadata containing a column
with sample IDs matching beta table column names.}

\item{annotation_file}{Path to the EPIC manifest/annotation CSV.}

\item{perfect_matches_file}{Optional CSV of probe IDs to retain (e.g., perfect
rhesus matches). If provided, only probes in this file are kept.}

\item{beta_probe_id_col}{Column name in \code{beta_file} containing probe IDs.}

\item{anno_probe_id_col}{Column name in \code{annotation_file} for probe IDs.}

\item{anno_chr_col}{Column name in \code{annotation_file} for chromosome.}

\item{anno_pos_col}{Column name in \code{annotation_file} for probe position.}

\item{anno_gene_col}{Optional column name in \code{annotation_file} for gene IDs.}

\item{sample_id_col}{Column name in \code{sample_info_file} listing sample IDs.}

\item{liftover_chain}{Optional path to a UCSC chain file used for liftover.}

\item{liftover_seqstyle}{Sequence naming style (e.g., \code{"UCSC"}).}

\item{probe_width}{Integer probe window size (bp) used for liftover. Defaults to 40.}

\item{coverage_per_probe}{Integer pseudo-coverage used to convert beta to counts.}

\item{write_outputs}{Logical; if TRUE, writes \code{<prefix>_MCounts.txt} and
\code{<prefix>_TCounts.txt}.}

\item{prefix}{Output prefix used when writing files.}

\item{return_annotation}{Logical; if TRUE, return probe annotation used.}
}
\value{
A list with:
\describe{
\item{MCounts}{data.frame with \code{chr}, \code{start}, and sample columns}
\item{TCounts}{data.frame with \code{chr}, \code{start}, and sample columns}
\item{annotation}{(optional) data.frame of probe coordinates and gene IDs}
}
}
\description{
Reads EPIC (Infinium) methylation array beta values and converts them into
pseudo-count matrices compatible with Mosasaur's downstream modeling
(e.g., PQLseq2). Probe coordinates are obtained from a manifest/annotation file
and can optionally be lifted over to a target genome using a UCSC chain file.
}
\details{
Conversion uses a constant pseudo-coverage per probe per sample
(\code{coverage_per_probe}):
\itemize{
\item \code{TCounts} = \code{coverage_per_probe}
\item \code{MCounts} = \code{round(beta * coverage_per_probe)}
}
}
\examples{
\dontrun{
out <- read_epic_array(
  beta_file = "EPIC_beta.txt",
  sample_info_file = "EPIC_samples.csv",
  annotation_file = "EPIC_manifest.csv",
  perfect_matches_file = "Perfect_MM_Matches.csv",
  liftover_chain = "hg19ToRheMac10.over.chain",
  prefix = "EPIC"
)
}

}
