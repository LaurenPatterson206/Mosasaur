% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/merge_data.R
\name{merge_data}
\alias{merge_data}
\title{Merge bedMethyl files into a single unified table}
\usage{
merge_data(path = ".", pattern = "\\\\mods.bed$")
}
\arguments{
\item{path}{Character string. Directory containing bedMethyl files.
Defaults to the working directory (\code{"."}).}

\item{pattern}{Regular expression pattern used to identify bedMethyl files.
Default matches files ending in \code{"\\\\mods.bed$"}.}
}
\value{
A merged \code{data.frame} with one row per CpG site and columns for
coverage, methylation, unmethylation, and hydroxymethylation for each sample.
}
\description{
Reads multiple bedMethyl (\verb{*.mods.bed}) files, extracts coverage and
methylation-related columns, renames them with sample-specific prefixes,
and merges all samples by genomic coordinate.
}
\details{
This function:
\itemize{
\item Identifies all files in \code{path} matching \code{pattern}
\item Reads each file using \code{data.table::fread()}
\item Keeps only rows where \code{V4 == "m"} (methylated sites)
\item Constructs a \code{Coord} variable in the format \code{"chr:start-end"}
\item Extracts and renames relevant measurement columns
\item Merges all samples using a full join on \code{Coord}
}
}
\examples{
\dontrun{
merged_df <- merge_data(path = "data/", pattern = "\\\\mods.bed$")
}

}
