% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_bisulfite_counts.R
\name{read_bisulfite_counts}
\alias{read_bisulfite_counts}
\title{Read bisulfite methylation count files and merge into Mosasaur format}
\usage{
read_bisulfite_counts(
  path = ".",
  pattern = "\\\\.txt$",
  files = NULL,
  sample_names = NULL,
  chr_col = "chr",
  start_col = "start",
  end_col = "end",
  meth_col = "meth",
  unmeth_col = "unmeth",
  coord_style = c("chr:start-end", "chr:start"),
  wide = FALSE,
  wide_file = NULL,
  return_coord_cols = FALSE
)
}
\arguments{
\item{path}{Directory containing per-sample files. Ignored if \code{files} provided.}

\item{pattern}{Regex used to match per-sample files (e.g., "\\\\.txt$").}

\item{files}{Optional character vector of file paths to read.}

\item{sample_names}{Optional sample names (same length as \code{files}).
If NULL, inferred from filename prefix before first dot.}

\item{chr_col, start_col, end_col}{Column names for genomic coordinates
in per-sample files.}

\item{meth_col, unmeth_col}{Column names for methylated/unmethylated counts
in per-sample files.}

\item{coord_style}{Either \code{"chr:start-end"} or \code{"chr:start"}.}

\item{wide}{Logical; if TRUE, reads a single wide table with sample columns
already present (see Details).}

\item{wide_file}{Path to a single wide-format table (required if \code{wide=TRUE}).}

\item{return_coord_cols}{Logical; if TRUE, also return \code{chr,start,end}.}
}
\value{
A merged \code{data.frame} with one row per CpG coordinate and
sample-specific columns for coverage/methyl/unmethyl/hydroxy (hydroxy=0).
}
\description{
Reads bisulfite sequencing count tables containing methylated and unmethylated
read counts, standardizes them, and merges across samples by genomic coordinate.
This function is intended to provide an entry point for bisulfite-based data
analogous to \code{\link{merge_data}} for nanopore bedMethyl.
}
\details{
Supported inputs:
\itemize{
\item \strong{Per-sample files (recommended)} with columns like:
\code{chr, start, end, meth, unmeth} (column names configurable)
\item \strong{A single wide file} containing multiple samples as columns
(use \code{wide = TRUE})
}

The returned merged table follows the same repeating 4-column block layout used
by Mosasaur's nanopore merge step:
\preformatted{
Coord | Coverage_sample | Methyl_sample | Unmethyl_sample | Hydroxymethyl_sample
}
For bisulfite data, Hydroxymethyl columns are filled with zeros, and Coverage is
computed as \code{meth + unmeth}.
}
\examples{
\dontrun{
merged_bs <- read_bisulfite_counts(
  path = "bisulfite_counts/",
  pattern = "\\\\\\\\.counts\\\\.txt$",
  chr_col = "chr",
  start_col = "pos",
  end_col = "pos_end",
  meth_col = "mC",
  unmeth_col = "uC"
)
}

}
